apiVersion: v1
kind: Service

metadata:
  name: rate-limiter-service
  namespace: rate-limiter

spec:
  selector:
    app: rate-limiter

  ports:
    - port: 3000
      targetPort: 3000

  type: NodePort